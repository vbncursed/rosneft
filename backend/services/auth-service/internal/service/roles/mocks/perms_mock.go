// Code generated by http://github.com/gojuno/minimock (v3.4.7). DO NOT EDIT.

package mocks

//go:generate minimock -i github.com/vbncursed/rosneft/backend/services/auth-service/internal/service/roles.Perms -o perms_mock.go -n PermsMock -p mocks

import (
	"context"
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
	"github.com/vbncursed/rosneft/backend/services/auth-service/internal/domain"
)

// PermsMock implements mm_roles.Perms
type PermsMock struct {
	t          minimock.Tester
	finishOnce sync.Once

	funcList          func(ctx context.Context) (pa1 []domain.Permission, err error)
	funcListOrigin    string
	inspectFuncList   func(ctx context.Context)
	afterListCounter  uint64
	beforeListCounter uint64
	ListMock          mPermsMockList
}

// NewPermsMock returns a mock for mm_roles.Perms
func NewPermsMock(t minimock.Tester) *PermsMock {
	m := &PermsMock{t: t}

	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.ListMock = mPermsMockList{mock: m}
	m.ListMock.callArgs = []*PermsMockListParams{}

	t.Cleanup(m.MinimockFinish)

	return m
}

type mPermsMockList struct {
	optional           bool
	mock               *PermsMock
	defaultExpectation *PermsMockListExpectation
	expectations       []*PermsMockListExpectation

	callArgs []*PermsMockListParams
	mutex    sync.RWMutex

	expectedInvocations       uint64
	expectedInvocationsOrigin string
}

// PermsMockListExpectation specifies expectation struct of the Perms.List
type PermsMockListExpectation struct {
	mock               *PermsMock
	params             *PermsMockListParams
	paramPtrs          *PermsMockListParamPtrs
	expectationOrigins PermsMockListExpectationOrigins
	results            *PermsMockListResults
	returnOrigin       string
	Counter            uint64
}

// PermsMockListParams contains parameters of the Perms.List
type PermsMockListParams struct {
	ctx context.Context
}

// PermsMockListParamPtrs contains pointers to parameters of the Perms.List
type PermsMockListParamPtrs struct {
	ctx *context.Context
}

// PermsMockListResults contains results of the Perms.List
type PermsMockListResults struct {
	pa1 []domain.Permission
	err error
}

// PermsMockListOrigins contains origins of expectations of the Perms.List
type PermsMockListExpectationOrigins struct {
	origin    string
	originCtx string
}

// Marks this method to be optional. The default behavior of any method with Return() is '1 or more', meaning
// the test will fail minimock's automatic final call check if the mocked method was not called at least once.
// Optional() makes method check to work in '0 or more' mode.
// It is NOT RECOMMENDED to use this option unless you really need it, as default behaviour helps to
// catch the problems when the expected method call is totally skipped during test run.
func (mmList *mPermsMockList) Optional() *mPermsMockList {
	mmList.optional = true
	return mmList
}

// Expect sets up expected params for Perms.List
func (mmList *mPermsMockList) Expect(ctx context.Context) *mPermsMockList {
	if mmList.mock.funcList != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by Set")
	}

	if mmList.defaultExpectation == nil {
		mmList.defaultExpectation = &PermsMockListExpectation{}
	}

	if mmList.defaultExpectation.paramPtrs != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by ExpectParams functions")
	}

	mmList.defaultExpectation.params = &PermsMockListParams{ctx}
	mmList.defaultExpectation.expectationOrigins.origin = minimock.CallerInfo(1)
	for _, e := range mmList.expectations {
		if minimock.Equal(e.params, mmList.defaultExpectation.params) {
			mmList.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmList.defaultExpectation.params)
		}
	}

	return mmList
}

// ExpectCtxParam1 sets up expected param ctx for Perms.List
func (mmList *mPermsMockList) ExpectCtxParam1(ctx context.Context) *mPermsMockList {
	if mmList.mock.funcList != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by Set")
	}

	if mmList.defaultExpectation == nil {
		mmList.defaultExpectation = &PermsMockListExpectation{}
	}

	if mmList.defaultExpectation.params != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by Expect")
	}

	if mmList.defaultExpectation.paramPtrs == nil {
		mmList.defaultExpectation.paramPtrs = &PermsMockListParamPtrs{}
	}
	mmList.defaultExpectation.paramPtrs.ctx = &ctx
	mmList.defaultExpectation.expectationOrigins.originCtx = minimock.CallerInfo(1)

	return mmList
}

// Inspect accepts an inspector function that has same arguments as the Perms.List
func (mmList *mPermsMockList) Inspect(f func(ctx context.Context)) *mPermsMockList {
	if mmList.mock.inspectFuncList != nil {
		mmList.mock.t.Fatalf("Inspect function is already set for PermsMock.List")
	}

	mmList.mock.inspectFuncList = f

	return mmList
}

// Return sets up results that will be returned by Perms.List
func (mmList *mPermsMockList) Return(pa1 []domain.Permission, err error) *PermsMock {
	if mmList.mock.funcList != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by Set")
	}

	if mmList.defaultExpectation == nil {
		mmList.defaultExpectation = &PermsMockListExpectation{mock: mmList.mock}
	}
	mmList.defaultExpectation.results = &PermsMockListResults{pa1, err}
	mmList.defaultExpectation.returnOrigin = minimock.CallerInfo(1)
	return mmList.mock
}

// Set uses given function f to mock the Perms.List method
func (mmList *mPermsMockList) Set(f func(ctx context.Context) (pa1 []domain.Permission, err error)) *PermsMock {
	if mmList.defaultExpectation != nil {
		mmList.mock.t.Fatalf("Default expectation is already set for the Perms.List method")
	}

	if len(mmList.expectations) > 0 {
		mmList.mock.t.Fatalf("Some expectations are already set for the Perms.List method")
	}

	mmList.mock.funcList = f
	mmList.mock.funcListOrigin = minimock.CallerInfo(1)
	return mmList.mock
}

// When sets expectation for the Perms.List which will trigger the result defined by the following
// Then helper
func (mmList *mPermsMockList) When(ctx context.Context) *PermsMockListExpectation {
	if mmList.mock.funcList != nil {
		mmList.mock.t.Fatalf("PermsMock.List mock is already set by Set")
	}

	expectation := &PermsMockListExpectation{
		mock:               mmList.mock,
		params:             &PermsMockListParams{ctx},
		expectationOrigins: PermsMockListExpectationOrigins{origin: minimock.CallerInfo(1)},
	}
	mmList.expectations = append(mmList.expectations, expectation)
	return expectation
}

// Then sets up Perms.List return parameters for the expectation previously defined by the When method
func (e *PermsMockListExpectation) Then(pa1 []domain.Permission, err error) *PermsMock {
	e.results = &PermsMockListResults{pa1, err}
	return e.mock
}

// Times sets number of times Perms.List should be invoked
func (mmList *mPermsMockList) Times(n uint64) *mPermsMockList {
	if n == 0 {
		mmList.mock.t.Fatalf("Times of PermsMock.List mock can not be zero")
	}
	mm_atomic.StoreUint64(&mmList.expectedInvocations, n)
	mmList.expectedInvocationsOrigin = minimock.CallerInfo(1)
	return mmList
}

func (mmList *mPermsMockList) invocationsDone() bool {
	if len(mmList.expectations) == 0 && mmList.defaultExpectation == nil && mmList.mock.funcList == nil {
		return true
	}

	totalInvocations := mm_atomic.LoadUint64(&mmList.mock.afterListCounter)
	expectedInvocations := mm_atomic.LoadUint64(&mmList.expectedInvocations)

	return totalInvocations > 0 && (expectedInvocations == 0 || expectedInvocations == totalInvocations)
}

// List implements mm_roles.Perms
func (mmList *PermsMock) List(ctx context.Context) (pa1 []domain.Permission, err error) {
	mm_atomic.AddUint64(&mmList.beforeListCounter, 1)
	defer mm_atomic.AddUint64(&mmList.afterListCounter, 1)

	mmList.t.Helper()

	if mmList.inspectFuncList != nil {
		mmList.inspectFuncList(ctx)
	}

	mm_params := PermsMockListParams{ctx}

	// Record call args
	mmList.ListMock.mutex.Lock()
	mmList.ListMock.callArgs = append(mmList.ListMock.callArgs, &mm_params)
	mmList.ListMock.mutex.Unlock()

	for _, e := range mmList.ListMock.expectations {
		if minimock.Equal(*e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.pa1, e.results.err
		}
	}

	if mmList.ListMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmList.ListMock.defaultExpectation.Counter, 1)
		mm_want := mmList.ListMock.defaultExpectation.params
		mm_want_ptrs := mmList.ListMock.defaultExpectation.paramPtrs

		mm_got := PermsMockListParams{ctx}

		if mm_want_ptrs != nil {

			if mm_want_ptrs.ctx != nil && !minimock.Equal(*mm_want_ptrs.ctx, mm_got.ctx) {
				mmList.t.Errorf("PermsMock.List got unexpected parameter ctx, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
					mmList.ListMock.defaultExpectation.expectationOrigins.originCtx, *mm_want_ptrs.ctx, mm_got.ctx, minimock.Diff(*mm_want_ptrs.ctx, mm_got.ctx))
			}

		} else if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmList.t.Errorf("PermsMock.List got unexpected parameters, expected at\n%s:\nwant: %#v\n got: %#v%s\n",
				mmList.ListMock.defaultExpectation.expectationOrigins.origin, *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		mm_results := mmList.ListMock.defaultExpectation.results
		if mm_results == nil {
			mmList.t.Fatal("No results are set for the PermsMock.List")
		}
		return (*mm_results).pa1, (*mm_results).err
	}
	if mmList.funcList != nil {
		return mmList.funcList(ctx)
	}
	mmList.t.Fatalf("Unexpected call to PermsMock.List. %v", ctx)
	return
}

// ListAfterCounter returns a count of finished PermsMock.List invocations
func (mmList *PermsMock) ListAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmList.afterListCounter)
}

// ListBeforeCounter returns a count of PermsMock.List invocations
func (mmList *PermsMock) ListBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmList.beforeListCounter)
}

// Calls returns a list of arguments used in each call to PermsMock.List.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmList *mPermsMockList) Calls() []*PermsMockListParams {
	mmList.mutex.RLock()

	argCopy := make([]*PermsMockListParams, len(mmList.callArgs))
	copy(argCopy, mmList.callArgs)

	mmList.mutex.RUnlock()

	return argCopy
}

// MinimockListDone returns true if the count of the List invocations corresponds
// the number of defined expectations
func (m *PermsMock) MinimockListDone() bool {
	if m.ListMock.optional {
		// Optional methods provide '0 or more' call count restriction.
		return true
	}

	for _, e := range m.ListMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	return m.ListMock.invocationsDone()
}

// MinimockListInspect logs each unmet expectation
func (m *PermsMock) MinimockListInspect() {
	for _, e := range m.ListMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to PermsMock.List at\n%s with params: %#v", e.expectationOrigins.origin, *e.params)
		}
	}

	afterListCounter := mm_atomic.LoadUint64(&m.afterListCounter)
	// if default expectation was set then invocations count should be greater than zero
	if m.ListMock.defaultExpectation != nil && afterListCounter < 1 {
		if m.ListMock.defaultExpectation.params == nil {
			m.t.Errorf("Expected call to PermsMock.List at\n%s", m.ListMock.defaultExpectation.returnOrigin)
		} else {
			m.t.Errorf("Expected call to PermsMock.List at\n%s with params: %#v", m.ListMock.defaultExpectation.expectationOrigins.origin, *m.ListMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcList != nil && afterListCounter < 1 {
		m.t.Errorf("Expected call to PermsMock.List at\n%s", m.funcListOrigin)
	}

	if !m.ListMock.invocationsDone() && afterListCounter > 0 {
		m.t.Errorf("Expected %d calls to PermsMock.List at\n%s but found %d calls",
			mm_atomic.LoadUint64(&m.ListMock.expectedInvocations), m.ListMock.expectedInvocationsOrigin, afterListCounter)
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *PermsMock) MinimockFinish() {
	m.finishOnce.Do(func() {
		if !m.minimockDone() {
			m.MinimockListInspect()
		}
	})
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *PermsMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *PermsMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockListDone()
}
