# Local dev points at the local backend gateway (CORS "*" in local compose).
# Production sets this to the empty string in .env.production: nginx serves the
# SPA and proxies /api to the gateway, so requests must be relative. Do not put
# an absolute production host here — there is no api.andrey.* origin.
VITE_API_URL=http://localhost:8080
